mkt_int_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: ${POSTGRES_HOST}
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      port: 5432
      dbname: ${POSTGRES_DB}
      schema: public