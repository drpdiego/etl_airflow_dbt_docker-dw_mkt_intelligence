POSTGRES_DB=***
POSTGRES_PASSWORD=***
POSTGRES_USER=***
# Maintain 'postgres' as the hostname and '5432' as the port for Docker networking
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

AIRFLOW_DB=***
AIRFLOW_DB_USER=***
AIRFLOW_DB_PASSWORD=***
# Maintain 'postgres_airflow' as the hostname and '5432' as the port for Docker networking
AIRFLOW_DB_HOST=postgres_airflow
AIRFLOW_DB_PORT=5432

AIRFLOW_USER=***
AIRFLOW_PASSWORD=***
AIRFLOW_FIRST_NAME=***
AIRFLOW_LAST_NAME=***
AIRFLOW_EMAIL=***

# Use the path bellow or put another path (out of this project) where is your data
SPREADSHEET_PATH=.\data\raw
